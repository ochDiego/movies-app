import Select from "./Select";

const MoviePage = () => {
  return (
    <div
      style={{
        padding: "10px 50px",
        background: "#fff",
        borderTop: "5px solid #999",
      }}
    >
      <Select />
    </div>
  );
};
export default MoviePage;
